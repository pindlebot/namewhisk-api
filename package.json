{
  "name": "namewhisk-redis",
  "version": "0.0.1",
  "main": "app.js",
  "license": "MIT",
  "dependencies": {
    "redis": "^2.8.0",
    "whois-2": "^0.0.7"
  },
  "devDependencies": {},
  "scripts": {
    "deploy": "up-env load && up deploy staging"
  }
}
